# Tests

This directory is reserved for tests once `src/agentic_dev_os/` contains executable helper code.

No test framework is required for the bootstrap version because the first deliverable is repository structure and Markdown artifacts.
