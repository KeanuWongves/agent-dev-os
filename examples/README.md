# Examples

This directory is reserved for example projects that demonstrate the full artifact chain:

```text
PRD -> MVP -> roadmap -> system design -> master plan -> task board
    -> task contract -> execution plan -> review report -> validation status
```

Examples should be small, complete, and useful for future agents validating the workflow.
